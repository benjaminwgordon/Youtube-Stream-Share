<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Public Streams</title>
</head>
<body>
    <div>
        <form method="POST" action="/rooms/">
            <input type="submit" value="Create a Room">
        </form>
    </div>
    <div>
        <ul>
            <% if (rooms.length == 0 ) { %>
                <p>No public rooms found</p>
            <% } %>
            <% for (room of rooms) { %>
                <li><a href=<%= `/rooms/${room}` %>>room.name</a></li>
            <% } %>
        </ul>
    </div>
</body>
</html>